<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roommake.channel.mapper.ChannelMapper">

    <insert id="insertChannel" parameterType="com.roommake.channel.vo.Channel">
        insert into channel
            (user_id, channel_title, channel_description, channel_image_name)
        values (#{user.id}, #{title}, #{description}, #{imageName})
    </insert>

</mapper>